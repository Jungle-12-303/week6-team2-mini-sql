# 11_main

이 폴더의 목표는 **전체 흐름을 연결하는 시작점**을 만드는 것입니다.

## 이 단계에서 만드는 파일

- `main.c`

## 이 파일이 하는 일

`main.c`는 아래 흐름을 한 번에 연결합니다.

1. SQL 파일 경로를 받는다.
2. 파일 내용을 읽는다.
3. `parse_sql()`로 분석한다.
4. `execute_command()`로 실행한다.
5. 성공 또는 에러를 출력하고 종료한다.

즉, 지금까지 만든 파일들을 실제 실행 순서대로 묶는 중심 파일입니다.
